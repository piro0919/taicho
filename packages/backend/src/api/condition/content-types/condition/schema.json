{
  "kind": "collectionType",
  "collectionName": "conditions",
  "info": {
    "singularName": "condition",
    "pluralName": "conditions",
    "displayName": "Conditions"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "uid": {
      "type": "string",
      "required": true
    },
    "date": {
      "type": "date",
      "required": true
    },
    "feeling": {
      "type": "enumeration",
      "enum": ["excellent", "veryGood", "good", "average", "poor"],
      "required": true
    },
    "physicalCondition": {
      "type": "enumeration",
      "enum": ["good", "average", "poor"],
      "required": true
    },
    "remark": {
      "type": "text"
    }
  }
}
